# {summary}

**日期**：{YYYY-MM-DD}
**作者**：{git user.name}
**分支**：{branch} → {base-branch}
**涉及提交**：{commit hashes 列表，或"工作区未提交改动"}
**涉及文件数**：{N}（+{add} / ~{modify} / -{delete}）

---

## 一、本次修改总结

> 一句话答案：**这次改了什么？为什么改？**

- 修改背景 / 触发原因（issue / bug / 需求 / 主动重构）：
- 核心修改逻辑（按模块/层级分点列出，每点配文件路径）：
  - `path/to/file.ts`：
  - `path/to/other.ts`：
- 关键设计选择（如果是多选一，说明为什么选了这个）：

## 二、给后人快速理解

> 目标：让后续接手的同学在 **5 分钟** 内 get 到关键点。

- **设计动机与权衡**：为什么这么做，不那么做？
- **关键概念 / 术语**：本次引入或重新定义的术语
- **数据流 / 调用链**：（必要时配 mermaid 图）

  ```mermaid
  flowchart LR
    A[入口] --> B[处理]
    B --> C[出口]
  ```

- **潜在副作用 / 已知限制**：
- **相关上下文**：issue / PR / 设计文档链接

## 三、给协作方

> 涉及接口、数据契约、前后端协议、SDK 行为变化时**必填**；纯内部重构填 "N/A"。

### 受影响的接口

| 接口 | 方法 | 变更类型 | 兼容性 |
|------|------|----------|--------|
| `/api/example` | GET | 新增 / 修改 / 废弃 | 向后兼容 / 破坏性 |

### 调用示例

**请求**：

```http
POST /api/example HTTP/1.1
Content-Type: application/json

{
  "field": "value"
}
```

**响应（成功）**：

```json
{
  "code": 0,
  "data": {}
}
```

**响应（失败）**：

```json
{
  "code": 40001,
  "message": "错误描述"
}
```

### 字段说明

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| `field` | string | 是 | — | — |

### 客户端 / 调用方需要做的改动

- [ ] 行动项 1
- [ ] 行动项 2

**上线节奏建议**：（同时上 / 先后端 / 先客户端 / 灰度策略）

## 四、Review（潜在问题与优化指南）

> 给未来的自己留一份"已知坑"。

- **潜在问题**
  - [ ] 并发 / race condition：
  - [ ] 性能（大数据量、慢查询）：
  - [ ] 边界 / 空值 / 错误处理：
  - [ ] 安全（鉴权、注入、越权）：
  - [ ] 兼容性（旧版本客户端、旧数据）：
- **测试覆盖盲区**：
- **短期优化建议**：
- **长期演进方向**：
- **监控 / 灰度建议**：
